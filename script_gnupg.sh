# A2 - Check GPG version
gpg --version#!/bin/bash
# =============================================================================
# BLOCK A — Environment Setup
# =============================================================================

# A1 - Verify GPG is installed
which gpg
